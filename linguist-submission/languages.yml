# Entry to add to lib/linguist/languages.yml
# Insertion point: alphabetical, after "Turing" and before "Turtle"
#
# The language_id is generated by running script/update-ids after
# adding the entry without a language_id field. The value below
# (847291) is a placeholder chosen to avoid collisions; the real
# value will be assigned by the script.

toke:
  type: programming
  color: "#c792ea"
  extensions:
    - ".tk"
  tm_scope: source.toke
  ace_mode: text
  language_id: 847291
