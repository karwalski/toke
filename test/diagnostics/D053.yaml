id: D053
series: D
description: "E4032 — interpolating a bare array-local variable is caught at --check (123.11-fu)"
input: |
  m=test;
  i=str:std.str;
  f=main():i64{
    let a=@(1;2;3);
    <str.len("a=\(a)")
  };
expected_exit_code: 1
expected_error_codes: ["E4032"]
