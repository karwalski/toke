id: D023
series: D
description: "Single-variant sum type match covers the one variant (exhaustive, no error)"
input: |
  m=test;
  t=$wrapper{$val:i64};
  f=unwrap(w:$wrapper):i64{
    <mt w{$val:v v}
  };
expected_exit_code: 0
expected_error_codes: []
