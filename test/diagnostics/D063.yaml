id: D063
series: D
description: "lint string-concat-chain: with TKC_LINT_CONCAT_FIX=1 a chain whose leaf is a call is still flagged but carries no fix"
input: |
  m=t;
  i=s:std.str;
  f=main():i64{
    let a="a";
    let b="b";
    let c=s.concat(s.concat(a;s.trim(b));"c");
    <s.len(c)
  };
flags: --lint
env: TKC_LINT_CONCAT_FIX=1
expected_exit_code: 0
expected_error_codes: ["string-concat-chain"]
expected_fix_absent_for: ["string-concat-chain"]
