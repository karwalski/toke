id: D040
series: D
description: "Non-exhaustive match on sum type in default mode emits E4010"
input: |
  m=test;
  t=$shape{$circle:i64;$square:i64;$triangle:i64};
  f=area(s:$shape):i64{
    <mt s{$circle:r r;$square:s2 s2}
  };
expected_exit_code: 1
expected_error_codes: ["E4010"]
