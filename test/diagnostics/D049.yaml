id: D049
series: D
description: "Returning a function-local primitive from a nested if block is safe (no E5001 — E5001 is arena-escape only)"
input: |
  m=test;
  f=bad(cond:bool):i64{
    if(cond){
      let inner=99;
      <inner
    }el{
      <0
    }
  };
expected_exit_code: 0
expected_error_codes: []
