id: D059
series: D
description: "lint flag-soup: two consecutive ifs assigning the same variable a literal are flagged once; mut-flag-if stays silent; not fixable"
input: |
  m=t;
  f=main():i64{
    let a=3;
    let b=4;
    let ok=mut.0;
    if(a>0){ok=1};
    if(b>0){ok=1};
    <ok
  };
flags: --lint
expected_exit_code: 0
expected_error_codes: ["flag-soup"]
expected_absent_codes: ["mut-flag-if"]
expected_fix_absent: true
