{
 "name": "DEV-045",
 "kind": "library",
 "reformatted": true,
 "applied": [
  {
   "id": "acc-array",
   "sites": 1,
   "note": "lines=lines+@(...) -> lines.append(...)"
  },
  {
   "id": "fn-chain-vs-let",
   "sites": 3,
   "note": "`l`, `isempty`, `iscomment` single-use lets inlined"
  },
  {
   "id": "cond-bool-combine",
   "sites": 2,
   "note": "empty/comment skip: `if(isempty){}el{if(iscomment){}el{...}}` -> `if(!(len==0)&&!startswith(#))`; DL3009: nested if with empty then-branch -> `&&!`"
  },
  {
   "id": "cond-elif-chain",
   "sites": 1,
   "note": "RUN look-ahead: nested if/el ladder -> el if chain"
  },
  {
   "id": "str-interp-vs-join",
   "sites": 4,
   "note": "three warning lines and the summary: nested s.concat -> interpolation"
  },
  {
   "id": "str-num-format",
   "sites": 4,
   "note": "s.fromint(i+1) x3, s.fromint(count) -> \"\\(..)\""
  }
 ],
 "not_applied": [
  {
   "id": "iter-find-first",
   "issue": null,
   "reason": "`found` flag + br in the RUN look-ahead is inline in main; direct return needs its own function"
  }
 ],
 "note": "canon laid out from `tkc --fmt` of the original; the formatter's `@($str)`->`@(str)` (also f64/i64/bool) rewrite was reverted by hand because `@(str)` fails codegen (E9003 `use of undefined value '%str'`) \u2014 the fmt'd original itself does not build"
}
