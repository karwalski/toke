{
 "name": "SOC-139",
 "kind": "library",
 "reformatted": true,
 "applied": [
  {
   "id": "str-interp-vs-join",
   "sites": 2,
   "note": "getstr pat (nested s.concat) and the 6-step out=s.concat chain -> one interpolation"
  },
  {
   "id": "str-num-format",
   "sites": 1,
   "note": "s.fromint(total) -> \\(total)"
  }
 ],
 "not_applied": []
}
