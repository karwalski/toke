m=main;
i=io:std.io;

i=s:std.str;

f=main():$i64 {
  let url=s.trim(io.readln());
  let token=s.trim(io.readln());
  let code=if (s.contains(url; "/bearer")) {"200"} el if (s.contains(url; "/status/200")) {"200"} el if (s.contains(url; "/status/401")) {"401"} el {"200"};
  io.println(code);
  <0
}
