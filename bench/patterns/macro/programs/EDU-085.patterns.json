{
 "name": "EDU-085",
 "kind": "library",
 "reformatted": true,
 "applied": [
  {
   "id": "str-build-loop",
   "sites": 1,
   "note": "stdin accumulation `js=s.concat(js;l)` -> builder"
  },
  {
   "id": "fn-chain-vs-let",
   "sites": 2,
   "note": "`let l=io.readln()` inlined into s.add; `numstr` inlined into s.tofloat(s.trim(...))"
  },
  {
   "id": "acc-array",
   "sites": 5,
   "note": "names/sums/counts/avgs/used: `x=x+@(v)` -> `x=x.append(v)` (typed `mut.@($str)` etc. literals keep the element type)"
  },
  {
   "id": "cond-bool-combine",
   "sites": 1,
   "note": "best-average selection: three nested ifs -> `!used.get(j)&&(best<0||avgs.get(j)>avgs.get(best))`"
  },
  {
   "id": "str-interp-vs-join",
   "sites": 1,
   "note": "rank line: 4 nested s.concat -> one interpolation (element of an .append-built @($str) interpolates correctly here)"
  },
  {
   "id": "str-num-format",
   "sites": 1,
   "note": "s.fromint(rank) -> \"\\(rank)\""
  }
 ],
 "not_applied": [
  {
   "id": "iter-find-first",
   "issue": null,
   "reason": "the `found` scan is inline in main and (deliberately) keeps the last match; no direct-return form"
  },
  {
   "id": "parse-json",
   "issue": null,
   "reason": "the program is a hand-rolled scanner over nested subject_scores objects; replacing it with json.dec would rewrite the whole program, not a pattern site (json.arr returns $err per the catalogue's applicability note)"
  }
 ],
 "note": "canon laid out from `tkc --fmt` of the original; the formatter's `@($str)`->`@(str)` (also f64/i64/bool) rewrite was reverted by hand because `@(str)` fails codegen (E9003 `use of undefined value '%str'`) \u2014 the fmt'd original itself does not build"
}
